import { ChaosSpec } from "./types";
import { DelayNetworkSettings } from "../../../sharedTypes";
export declare class ChaosResource {
    protected readonly name: string;
    protected readonly namespace: string;
    protected readonly delay: DelayNetworkSettings;
    constructor(name: string, namespace: string, delay: DelayNetworkSettings);
    generateSpec(): ChaosSpec;
    private generateChaosSpec;
}
