declare function checkExists(path: string): Promise<boolean>;
declare function downloadBinsIfMissing(binPath: string): Promise<void>;
declare function checkListeningPorts(processId: number): {
    binName: string;
    processId: number;
    ports: string[];
};
declare function checkAlreadyRunning(binaryName: string): number[];
declare function promptAlreadyRunning(pids: number[]): Promise<void>;
declare function checkAccess(path: string): void;

export { checkAccess, checkAlreadyRunning, checkExists, checkListeningPorts, downloadBinsIfMissing, promptAlreadyRunning };
