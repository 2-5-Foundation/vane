import { Environment } from '@moonwall/types';
import { Vitest } from 'vitest';

declare function testCmd(envName: string, additionalArgs?: object): Promise<boolean>;
declare function executeTests(env: Environment, additionalArgs?: object): Promise<Vitest>;

export { executeTests, testCmd };
