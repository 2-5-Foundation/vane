import { ZombieLaunchSpec, DevLaunchSpec, RepoSpec, ChopsticksLaunchSpec } from '@moonwall/types';

declare function parseZombieCmd(launchSpec: ZombieLaunchSpec): {
    cmd: string;
};
declare function parseRunCmd(launchSpec: DevLaunchSpec, additionalRepos?: RepoSpec[]): {
    cmd: string;
    args: string[];
    launch: true;
};
declare function parseChopsticksRunCmd(launchSpecs: ChopsticksLaunchSpec[]): {
    cmd: string;
    args: string[];
    launch: boolean;
};

export { parseChopsticksRunCmd, parseRunCmd, parseZombieCmd };
