import { FoundationType, MoonwallConfig } from '@moonwall/types';

declare function createFolders(): Promise<void>;
declare function generateConfig(): Promise<void>;
declare function createConfig(options: {
    label: string;
    timeout: number;
    environmentName: string;
    foundation: FoundationType;
    testDir: string;
}): MoonwallConfig;

export { createConfig, createFolders, generateConfig };
