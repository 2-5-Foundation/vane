import { Environment } from '@moonwall/types';

declare function commonChecks(env: Environment): Promise<void>;
declare function executeScript(scriptCommand: string, args?: string): Promise<void>;

export { commonChecks, executeScript };
