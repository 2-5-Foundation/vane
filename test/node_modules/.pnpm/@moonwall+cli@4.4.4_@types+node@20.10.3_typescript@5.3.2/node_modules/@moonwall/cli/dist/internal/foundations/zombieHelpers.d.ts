import { LaunchConfig } from '@zombienet/orchestrator';

declare function checkZombieBins(config: LaunchConfig): Promise<void>;
declare function getZombieConfig(path: string): LaunchConfig;
type IPCRequestMessage = {
    text: string;
    cmd: "restart" | "pause" | "resume" | "kill" | "isup" | "init" | "networkmap";
    nodeName?: string;
};
type IPCResponseMessage = {
    status: "success" | "failure";
    result: boolean | object;
    message: string;
};
declare function sendIpcMessage(message: IPCRequestMessage): Promise<IPCResponseMessage>;

export { type IPCRequestMessage, type IPCResponseMessage, checkZombieBins, getZombieConfig, sendIpcMessage };
